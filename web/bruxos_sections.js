// Bruxos do VFX — divisorias DESLIGADAS.
// As versoes anteriores (overlay / DOM widget / label) ou nao apareciam no
// Nodes 2.0, ou deixavam o layout poluido, ou mexiam no rotulo (o que podia
// atrapalhar o alinhamento dos valores). Por decisao do usuario, nao ha mais
// divisorias — os campos ficam com o nome limpo, sem nada inserido.
console.log("[Bruxos] divisorias desligadas (layout limpo)");
